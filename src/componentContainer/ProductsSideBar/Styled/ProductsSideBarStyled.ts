import styled from 'styled-components'

export const ProductSideBarLabels = styled.div`
  height: 30px;
  font-size: ${(props) => props.theme.fontsSize.small};
  font-weight: 600;
  display: flex;
  align-items: center;
  padding: 0 10px;
`
